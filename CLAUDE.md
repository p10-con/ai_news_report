# AI ニュースリサーチ スケジュール実行ガイド

## 実行フロー（確認なし・自動完結）

毎日実行時に以下を自動実行。質問や確認は一切入れず、判断して進める。

---

## Step 1 — 実行日の曜日とテーマを確認

日本時間の曜日を確認し、対応するテーマと検索キーワードで調査を開始。

| 曜日 | テーマ | 主検索ワード | 補助ワード | 情報源優先度 |
|------|--------|------------|----------|-----------|
| **月** | 技術 + 実装 | `AI new model April 2026`, `Claude API update`, `LLM benchmark` | `Next.js AI integration`, `Python library release` | 公式ブログ＞論文＞ニュース |
| **火** | ビジネス + デザイン | `AI startup funding`, `Figma AI`, `design tool update` | `AI market share`, `enterprise AI adoption` | Crunchbase＞公式＞ニュース |
| **水** | 技術 + 実装 | `AI model release`, `API pricing update`, `ML benchmark` | `Three.js AI`, `WebGL generative` | GitHub releases＞公式＞論文 |
| **木** | ビジネス + デザイン | `AI business news`, `AI design tools`, `3D AI modeling` | `generative UI`, `Blender AI` | TechCrunch＞公式＞業界ニュース |
| **金** | 技術 + 実装 | `frontier model release`, `LLM library`, `AI infrastructure` | `inference optimization`, `open-source model` | 公式リリース＞論文＞GitHub |
| **土** | 週まとめ + 深掘り | 当週のレポートファイル 5 件を参照 | 深掘り候補から最重要 2 件選定 | 実装例＞ケーススタディ＞分析 |
| **日** | 自由探索 | `AI entertainment`, `generative art`, `interesting AI project` | テーマ自由 | 話題性＞個人ブログ＞公式 |

**確認項目**: `date` コマンドで日本時間の曜日を確認。曜日が不明な場合は、システム提供の日付から計算。

---

## Step 2 — Web 検索（最小 3 件以上のソース）

### 検索戦略

1. **主ワード×月份で検索** — 例: `Claude new model April 2026`
2. **補助ワード×月份で 1～2 回追加検索**
3. **計 3 セット以上の検索を実行**

### トピック選定ルール（重要度判断）

**以下の基準で重要度を判断し、トップ 3 を自動選定。質問しない。**

| 基準 | スコア | 判断方法 |
|------|--------|---------|
| **実務影響度** | 高(3) / 中(2) / 低(1) | フロントエンド・デザイン・3D での実装に直結するか |
| **技術成熟度** | GA/正式版(3) / β/プレビュー(2) / 実験(1) | リリース段階 |
| **ソース信頼度** | 公式(3) / ニュース(2) / ブログ(1) | 一次ソース度 |
| **新規性** | 1 週間以内(3) / 2～3 週間(2) / 1 ヶ月以上(1) | 情報の鮮度 |

**選定方法**: スコア合計が高い順に TOP 3 を選定。同点の場合は「実務影響度 > 新規性 > 信頼度 > 成熟度」の優先度で決定。

**決定後の迷いなし**: 一度選定したら、そのトピックで進める。「これで良いか」という確認はしない。

---

## Step 3 — ブランチ作成と日付ファイル作成

```bash
git checkout main
git pull origin main
git checkout -b news/YYYYMMDD
```

ファイル名: `YYYYMMDD.md`（例: `20260401.md`）

---

## Step 4 — レポートテンプレート（固定フォーマット）

```markdown
# YYYY-MM-DD AI ニュース（曜日・テーマ名）

## トピック 1 — 見出し（25～40 字）

URL

本文 3～5 行：
- 1 行目: 何が起きたか（事実）
- 2～3 行目: なぜ重要か（実務への影響）
- 4～5 行目: 実装・運用への示唆

## トピック 2 — 見出し

[同上]

## トピック 3 — 見出し

[同上]
```

**ルール**:
- 見出しは日本語、簡潔（事実 + 重要性を一言）
- URL は検索結果から取得した正確なもの
- 本文は日本語で 3～5 行、具体的な数値・日付を含める
- 重要度順に並べる（Top 3）

---

## Step 5 — README.md の更新（全置き換え）

```markdown
# AI News Log

最終更新: YYYY-MM-DD（曜日）

---

## 今週のハイライト

> 直近で最も重要なトピックを 3 件、一言コメント付きで列挙。
> 毎日更新し、より重要なニュースが出たら差し替える。

- **[トピック 1 の見出し + URL](URL)** — 重要性 1 言
- **[トピック 2 の見出し + URL](URL)** — 重要性 1 言
- **[トピック 3 の見出し + URL](URL)** — 重要性 1 言

---

## 今週のサマリー

※ 該当週が確定するまで、現在のサマリーを保持。土曜に完成予定。

---

## レポート一覧

| 日付 | 曜日 | テーマ | ファイル |
|------|------|--------|----------|
| YYYY-MM-DD | 曜 | テーマ | [YYYYMMDD.md](./YYYYMMDD.md) |
[既存行を上に追加・古い行は削除（最大 14 行＝2 週間分保持)]

---

## 深掘り候補（次の土曜用）

- トピック名 — 気になった理由メモ（新たに発見したものを追記）
[既存行も保持]
```

**更新ルール**:
- ハイライト: 今日の 3 トピックで完全置き換え
- レポート一覧: 本日のエントリを最上行に追加、2 週間（14 行）超過分は削除
- 深掘り候補: 本日のトピックから「次の土曜に掘るべき」ものを追記、既存は保持
- 最終更新日: YYYY-MM-DD（曜日）に更新

---

## Step 6 — コミット・プッシュ・PR・マージ（自動完結）

```bash
git add YYYYMMDD.md README.md
git commit -m "news: YYYYMMDD <見出し 1・見出し 2・見出し 3>"
git push -u origin news/YYYYMMDD
```

**PR 作成・マージ（GitHub MCP 使用）**:
1. `mcp__github__create_pull_request` で PR 作成
   - title: `news: YYYYMMDD <見出し 1・見出し 2・見出し 3>`
   - body: 調査日・ハイライト・変更ファイル
   - base: `main`
   - head: `news/YYYYMMDD`

2. `mcp__github__merge_pull_request` で即座にマージ
   - merge_method: `squash`

3. ブランチ削除 (`git branch -d`、リモート削除エラーは無視)

---

## 実行時の判断ルール（迷ったときのデフォルト）

| 状況 | 判断 | 理由 |
|------|------|------|
| トピック 3 つで迷った | **スコア合計が高い順に選ぶ**。同点なら「実務影響 > 新規性」優先 | 客観的判定で確認不要 |
| URL が複数見つかった | **最初に見つかった公式 URL を使う** | ソース信頼度優先 |
| 古い情報（1 ヶ月以上前）が出た | **その月の情報を優先。古い情報は除外** | 「最新」が要件 |
| テーマと関係ないニュースが出た | **該当テーマに絞る。外れたものは除外** | 曜日別フォーカス厳守 |
| 検索結果が 3 件未満 | **別の検索ワード（補助）を試す**。それでも不足なら補助ワードで追加検索 | 最小 3 件確保 |
| README の行数が多すぎる | **レポート一覧を 14 行（2 週間）以内に保つ** | スコープ管理 |

---

## 品質チェックリスト（実行後・自動チェック）

実行後、以下を self-check。問題があれば修正して再コミット。

- [ ] URL は実在し、アクセス可能か（検索結果のリンク）
- [ ] 日付は YYYYMMDD フォーマット統一されているか
- [ ] 見出しは日本語で 25～40 字、事実 + 重要性を含むか
- [ ] 本文は 3～5 行で、数値や日付を含むか
- [ ] README のハイライトは本日の 3 トピックに更新されているか
- [ ] レポート一覧は最新のエントリが最上行か
- [ ] 深掘り候補に当日発見したトピックが追記されているか

---

## 土曜日の特別フロー（深掘り実装）

土曜は「その週全体を代表する 1～2 トピック」を深掘り。

1. 当週のレポートファイル（月～金）5 つを全読
2. 深掘り候補から「最も議論の価値がある」2 件を選定
3. 各 2 トピックについて、追加リサーチ 3～5 件実施
4. README の「今週のサマリー」を 100～150 字で作成
5. 通常フロー（Step 3～6）に従う

---

## 実行時の "ストップポイント" を完全排除

**以下は実行しない。迷ったら判断ルールに従う。**

- ❌ 「このトピック選定で良いですか？」
- ❌ 「レポート内容は十分ですか？」
- ❌ 「README の更新は適切ですか？」
- ❌ 「PR をマージして良いですか？」

**すべて自動判定・自動実行。確認なし。**

---

## 実装例

### 月曜（技術 + 実装）の場合

```
git status  → clean 確認
日本時間 曜日確認 → 月曜
検索 1: "Claude new model April 2026"
検索 2: "Next.js AI SDK update"
検索 3: "Python LLM library April 2026"

トピック選定（スコアで判定）:
- Claude Opus 4.7 (score: 12)
- Next.js AI Agent (score: 11)
- vLLM update (score: 10)

ファイル作成 → コミット → プッシュ → PR → マージ
完了報告（確認なし）
```

---

## 日付・曜日の正確性

**重要**: 日本時間で曜日判定すること。

```bash
# 日本時間で確認（TZ=Asia/Tokyo で date 実行推奨）
TZ=Asia/Tokyo date
```

システム提供の date を use する場合、UTC との時差を勘案。

---

## まとめ

**このガイドに従えば、質問・確認なしに自動完結。**

- 検索ワード: テーブルから参照
- トピック選定: スコアルール（数値化）で判定
- レポート作成: テンプレート固定
- README 更新: 置き換えルール固定
- マージ: 自動実行
- 迷ったときの判断: ルール表を参照

「8 割で止まる」ことはない。

