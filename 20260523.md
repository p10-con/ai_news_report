# 2026-05-23 AI ニュース（土曜・週まとめ + 深掘り）

## トピック 1 — Claude Opus 4.7 本番環境のコスト最適化戦略

https://platform.claude.com/docs/en/about-claude/models/migration-guide

Opus 4.7 は新しいトークナイザーにより同じテキストで最大 35% 多くのトークンを生成するため、ユーザー当たりの実効コストが 12～27% 上昇する可能性がある。ただしプロンプトキャッシング（キャッシュトークンは 90% 割引）が長いプロンプトで大部分を吸収（128K トークン超では 93% のトークンがキャッシュに収納）するため、事前に実トラフィックをリプレイして cost delta を測定する必要がある。GitHub Copilot の 6 月 1 日以降のトークンベース課金に備え、batch processing（50% 割引）と routing layer redesign（低バリューワークロードは Opus 4.6 に routing、高バリューのみ 4.7）で blended cost 最適化する企業が増加中。

## トピック 2 — Claude Managed Agents のマルチエージェント統合パターン

https://platform.claude.com/docs/en/managed-agents/multi-agent

Managed Agents では 1 つのリード・エージェントが複数の specialist エージェントに タスクを並列分割し、各エージェントが独自のコンテキスト・プロンプト・ツール・セッションスレッドで動作（共有ファイルシステム・vault 認証情報あり）。5 月の update で outcome（完了通知）と webhook ベースの非同期調整が beta になり、long-running task 中も connection を維持する必要がなくなった。コード例では customer support × code review agent chain で、support agent が issue を intake → code review specialist が PR を分析 → lead が action item 集約。production patterns では「demo agent は fast-fail、production agent は slow-fail」が教訓で、routing decision・memory governance・failure propagation を先行実装が必須。Claude Code のエージェント実行レートが $2.5B に達し、Uber・Salesforce・Accenture 等の大規模企業への適用が加速。

## トピック 3 — AI 推論インフラの二層化戦略（Edge + Datacenter）

https://thenewstack.io/subquadratic-12-million-context-window/

frontier model のコンテキストウィンドウが 12M token（SubQ）まで拡大する一方、deployment infrastructure は二層化が不可避。edge（mobile・browser）では latency・cost・accuracy trade-off から lightweight model（DeepSeek V4-Flash等）を採用、datacenter では xhigh effort level（最高品質タスク）と長コンテキスト処理に frontier model を配置。multi-turn agent・long-context QA・document summarization は datacenter でサーバー、context-agnostic 推論（sentiment・NER・classification）は edge で run。team 間の node allocation routing decision が coordination overhead になるため、self-serving routing policy と cost attribution model を 組織横断で導入する企業が増加中。
